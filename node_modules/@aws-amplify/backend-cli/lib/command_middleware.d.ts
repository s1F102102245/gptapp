import { ArgumentsCamelCase } from 'yargs';
export declare const profileSetupInstruction = "To configure a new Amplify profile, use \"amplify configure profile\".";
/**
 * Contains middleware functions.
 */
export declare class CommandMiddleware {
    /**
     * Ensure AWS credentials and region of the input profile (or 'default' if undefined) are available in the provider chain.
     * If the input profile is defined, the environment variable AWS_PROFILE will be set accordingly.
     */
    ensureAwsCredentialAndRegion: <T extends {
        profile: string | undefined;
    }>(argv: ArgumentsCamelCase<T>) => Promise<void>;
}
//# sourceMappingURL=command_middleware.d.ts.map
import { AwsCredentialIdentityProvider } from '@aws-sdk/types';
import { ClientConfig } from './client-config-types/client_config.js';
import { DeployedBackendIdentifier } from '@aws-amplify/deployed-backend-client';
/**
 * Main entry point for generating client config
 */
export declare const generateClientConfig: (credentialProvider: AwsCredentialIdentityProvider, backendIdentifier: DeployedBackendIdentifier) => Promise<ClientConfig>;
//# sourceMappingURL=generate_client_config.d.ts.map
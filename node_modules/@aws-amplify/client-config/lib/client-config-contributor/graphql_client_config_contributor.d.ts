import { ClientConfigContributor } from './client_config_contributor.js';
import { UnifiedBackendOutput, graphqlOutputKey } from '@aws-amplify/backend-output-schemas';
import { GraphqlClientConfig } from '../client-config-types/graphql_client_config.js';
import { ModelIntrospectionSchemaAdapter } from './model_introspection_schema_adapater.js';
/**
 * Translator for the Graphql API portion of ClientConfig
 */
export declare class GraphqlClientConfigContributor implements ClientConfigContributor {
    private readonly modelIntrospectionSchemaAdapter;
    /**
     * Constructor
     * @param modelIntrospectionSchemaAdapter the adapter to provide the model introspection schema from s3 uri
     */
    constructor(modelIntrospectionSchemaAdapter: ModelIntrospectionSchemaAdapter);
    /**
     * Given some BackendOutput, contribute the Graphql API portion of the client config
     */
    contribute: ({ [graphqlOutputKey]: graphqlOutput, }: UnifiedBackendOutput) => Promise<GraphqlClientConfig | Record<string, never>>;
}
//# sourceMappingURL=graphql_client_config_contributor.d.ts.map
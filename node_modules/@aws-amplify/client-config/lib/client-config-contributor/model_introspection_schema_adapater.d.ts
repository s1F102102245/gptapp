import { AwsCredentialIdentityProvider } from '@aws-sdk/types';
/**
 * Adapts static getModelIntrospectionSchemaFromS3Uri from @aws-amplify/model-generator call to make it injectable and testable.
 */
export declare class ModelIntrospectionSchemaAdapter {
    private readonly awsCredentialProvider;
    /**
     * Creates new adapter for getModelIntrospectionSchemaFromS3Uri from @aws-amplify/model-generator.
     */
    constructor(awsCredentialProvider: AwsCredentialIdentityProvider);
    /**
     * Generates the client configuration for a given backend
     */
    getModelIntrospectionSchemaFromS3Uri: (modelSchemaS3Uri: string | undefined) => Promise<unknown | undefined>;
}
//# sourceMappingURL=model_introspection_schema_adapater.d.ts.map
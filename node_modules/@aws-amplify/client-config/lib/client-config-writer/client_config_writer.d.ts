/// <reference types="node" resolution-mode="require"/>
import _fsp from 'fs/promises';
import { ClientConfig, ClientConfigFormat } from '../client-config-types/client_config.js';
import { ClientConfigFormatter } from './client_config_formatter.js';
export type ClientConfigPathResolver = (outDir?: string, format?: ClientConfigFormat) => Promise<string>;
/**
 * A class that persists client config to disk.
 */
export declare class ClientConfigWriter {
    private readonly pathResolver;
    private readonly formatter;
    private readonly fsp;
    /**
     * Creates client config writer
     */
    constructor(pathResolver: ClientConfigPathResolver, formatter: ClientConfigFormatter, fsp?: typeof _fsp);
    /**
     * Persists provided client config as json file to target path.
     */
    writeClientConfig: (clientConfig: ClientConfig, outDir?: string, format?: ClientConfigFormat) => Promise<void>;
}
//# sourceMappingURL=client_config_writer.d.ts.map